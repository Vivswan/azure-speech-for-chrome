#!/bin/sh

set -e

if [ ! -d node_modules ]; then
	echo "node_modules is missing. Run 'bun install' before committing."
	exit 1
fi

bunx lint-staged
bunx prettier --check .
bun run lint:actions
bun run lint
bun run typecheck
bun run test:run
